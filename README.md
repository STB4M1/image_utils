# 🖼 image_utils -- Julia × Python マルチ言語画像処理ツールキット

**Julia / Python 両対応の画像処理・ファイル管理ユーティリティ**です。\
光学・流体・ホログラフィ・顕微鏡画像解析などの研究用途向けに、\
RGB・Gray・Bayer の高精度処理パイプラインを **全く同じ関数名・同じ挙動**
で扱える\
**マルチ言語画像処理ツールキット**として開発しています。

------------------------------------------------------------------------

# 📂 Repository Structure

    image_utils/
    │
    ├── 99_codes/                      # テスト画像生成 (Python)
    │   ├── 01_make_img_test.py        # FullHD RGB テキスト画像生成
    │   └── 02_make_Bayer.py           # RGGB/BGGR/GBRG/GRBG Bayer画像生成
    │
    ├── julia/
    │   ├── example/                   # Julia使用例 (RGB/Bayer/Dowsample)
    │   │   ├── main_bayer_downsample.jl
    │   │   ├── main_bayer.jl
    │   │   └── main_rgb.jl
    │   │
    │   ├── FSUtils/                   # Julia版 FSUtils（mkdir）
    │   │   ├── Project.toml
    │   │   └── src/FSUtils.jl
    │   │
    │   └── ImageUtils/                # Julia版 ImageUtils（画像処理）
    │       ├── Project.toml
    │       ├── Manifest.toml
    │       └── src/ImageUtils.jl
    │
    ├── python/
    │   ├── example/                   # Python使用例 (RGB/Bayer/Downsample)
    │   │   ├── main_bayer_downsample_all.py
    │   │   ├── main_bayer.py
    │   │   └── main_rgb.py
    │   │
    │   ├── fs_utils/                  # Python版 FSUtils (mkdir)
    │   │   ├── setup.py
    │   │   └── fs_utils/
    │   │       ├── __init__.py
    │   │       └── fs_utils.py
    │   │
    │   └── image_utils/               # Python版 ImageUtils（画像処理）
    │       ├── setup.py
    │       └── image_utils/
    │           ├── __init__.py
    │           └── image_utils.py
    │
    ├── test_images/                   # 入力画像（ignored）
    └── results/                       # 出力画像（ignored）

------------------------------------------------------------------------

# 🟣 画像処理ユーティリティ（Julia & Python 共通API）

Julia と Python どちらでも **同一の関数 API** が使えるように設計。\
研究コードを言語間で移植しても、ほぼコピペで動作します。

------------------------------------------------------------------------

## 🖼 読み込み

  関数名                          内容
  ------------------------------- -------------------------------
  `read_img_gray_float64(path)`   Gray画像を Float64 で読み込み
  `read_img_rgb_float64(path)`    RGB画像を Float64 で読み込み

------------------------------------------------------------------------

## 💾 保存（Gray / RGB）

  -----------------------------------------------------------------------
  関数名                                   内容
  ---------------------------------------- ------------------------------
  `save_gray_like_c(img,path)`             C言語 `unsigned char`
                                           と同じ挙動で保存

  `save_gray_standard(img,path)`           round + clamp の標準UInt8保存

  `save_rgb_like_c(R,G,B,path)`            RGB を C互換キャストで保存

  `save_rgb_standard(R,G,B,path)`          標準UInt8 RGB保存
  -----------------------------------------------------------------------

------------------------------------------------------------------------

## 🎨 RGB 操作

  --------------------------------------------------------------------------------------------
  関数名                                                        内容
  ------------------------------------------------------------- ------------------------------
  `separate_rgb(in, out, color; order, save_type, save_mode)`   RGBの特定チャンネル抽出

  --------------------------------------------------------------------------------------------

------------------------------------------------------------------------

## 🔴 Bayer 操作

  関数名                                 内容
  -------------------------------------- -----------------------------
  `raw_separate_bayer(bayer, pattern)`   Bayer → (R,G,B) 3ch展開
  `separate_bayer(...)`                  Bayerの特定チャンネルを抽出
  `separate_bayer_downsample(...)`       Downsample Bayer抽出

対応パターン：

    RGGB, BGGR, GBRG, GRBG

------------------------------------------------------------------------

# 🟡 ファイルユーティリティ（Julia & Python 共通API）

  -----------------------------------------------------------------------
  関数名                                   内容
  ---------------------------------------- ------------------------------
  `make_dirs(dir...)`                      mkdir -p（Julia: mkpath /
                                           Python: os.makedirs）

  -----------------------------------------------------------------------

------------------------------------------------------------------------

# 🧪 Test Image Generators (99_codes)

研究向けテスト画像生成ツール（Python）。

------------------------------------------------------------------------

## ◆ 01_make_img_test.py

-   FullHD RGB テキスト画像生成\
-   bbox補正で文字切れなし\
-   FullHDへ自動整形\
-   四隅に Bayer パターン埋め込み\
-   出力：`RGB_words_FullHD.png`

------------------------------------------------------------------------

## ◆ 02_make_Bayer.py

RGB FullHD 画像 → Bayer 4種を生成：

    bayer_fullHD_RGGB.png
    bayer_fullHD_BGGR.png
    bayer_fullHD_GBRG.png
    bayer_fullHD_GRBG.png

------------------------------------------------------------------------

# 📘 Examples

Julia と Python を
**全く同じ処理フロー**で使えるよう、対応するスクリプトを提供：

    julia/example/
    ├── main_rgb.jl
    ├── main_bayer.jl
    └── main_bayer_downsample.jl

    python/example/
    ├── main_rgb.py
    ├── main_bayer.py
    └── main_bayer_downsample_all.py

------------------------------------------------------------------------

# 📦 Installation

## Julia

``` julia
] dev path/to/image_utils/julia/ImageUtils
] dev path/to/image_utils/julia/FSUtils
```

## Python

    pip install -e .

使用例：

``` python
from image_utils import separate_bayer
from fs_utils import make_dirs
```

------------------------------------------------------------------------

# 📄 License

MIT License
