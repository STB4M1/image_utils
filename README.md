# image_utils

複数言語（**Julia /
Python**）対応の画像処理・ファイル管理ユーティリティをまとめた\
**マルチ言語ツールキット** です。

研究用途（光学・流体・ホログラフィ・顕微鏡画像解析など）から\
一般的な画像処理パイプラインまで利用可能な、\
自作パッケージ **ImageUtils / FSUtils** と\
Python スクリプト群（RGB画像生成 & Bayer生成）で構成されています。

------------------------------------------------------------------------

# 📂 Repository Structure

    image_utils/
     ├── julia/
     │    ├── ImageUtils/          # 画像処理 (RGB/Gray/Bayer 分離)
     │    └── FSUtils/             # ファイル管理 (mkdir)
     │
     ├── python/                   # ※今後追加予定の Python モジュール
     │
     ├── 99_codes/                 # テスト画像生成用 Python スクリプト
     │    ├── 01_make_img_test.py      # RGB単語画像 FullHD 作成 + Bayer マーク
     │    └── 02_make_Bayer.py         # 4 種類の Bayer 画像生成
     │
     ├── example/
     │    ├── julia/               # Julia使用例
     │    └── python/              # Python使用例（予定）
     │
     ├── test_images/              # 入力画像（ignored）
     └── results/                  # 出力画像（ignored）

------------------------------------------------------------------------

# 🟣 Julia Packages

## \## 🖼 ImageUtils（画像処理ユーティリティ）

RGB / Gray 読み込み、Bayer 分離、\
C言語"unsigned char"互換キャストでの保存、\
チャンネル抽出など、研究用途で使える高精度画像処理ツール。

### ✨ 主な関数

  -----------------------------------------------------------------------------------------
  関数名                                                   内容
  -------------------------------------------------------- --------------------------------
  `read_img_gray_float64(path)`                            Gray画像を Float64 で読み込み

  `read_img_rgb_float64(path)`                             RGB画像を Float64 で読み込み

  `save_gray_like_c(img, path)`                            C言語 unsigned char
                                                           と同じ挙動で保存

  `save_gray_standard(img,path)`                           標準的 round + clamp による
                                                           UInt8 保存

  `save_rgb_like_c(;R,G,B,path)`                           RGB を C互換キャストで保存

  `save_rgb_standard(;R,G,B,path)`                         標準的 UInt8 保存

  `separate_rgb(in,out,color;order,save_type,save_mode)`   RGB の特定チャンネルを抽出/保存

  `raw_separate_bayer(bayer,pattern)`                      Bayer パターンから R/G/Bを抽出

  `separate_bayer(…)`                                      Bayer の1ch抽出

  `separate_bayer_downsample(…)`                           Downsample した Bayer 抽出
  
  ------------------------------------------------------------------------
---

## 🟡 Python Scripts（99_codes/）

研究のためのテスト画像を自動生成する Python スクリプト群。

### ◆ 01_make_img_test.py

RGB の単語画像 + 四隅に Bayer マイクロパターンを描画し、\
**1920×1080 FullHD の画像を自動生成**。

#### ✨ 主な処理

-   Red / Green / Blue を高精度テキストレンダリング\
-   自動 bbox 補正（文字切れゼロ）
-   アスペクト比維持の FullHD リサイズ\
-   四隅に RGGB / GRBG / BGGR / GBRG の Bayer マークを描画\
-   `RGB_words_FullHD.png` を出力

### ◆ 02_make_Bayer.py

01 番で作った RGB_words_FullHD.png から\
**4 種類の Bayer パターン画像を生成**。

#### 作成される画像

-   bayer_fullHD_RGGB.png\
-   bayer_fullHD_BGGR.png\
-   bayer_fullHD_GBRG.png\
-   bayer_fullHD_GRBG.png

------------------------------------------------------------------------

# 📦 Installation (Julia)

``` julia
] dev path/to/image_utils/julia/ImageUtils
] dev path/to/image_utils/julia/FSUtils
```

-----------------------------------

## 📄 License
This project is licensed under the MIT License.
